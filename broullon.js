 /*if (element.innerText.includes(fastNfurious)) {
        // console.log(element.innerText)
        element
        unorganisedList.insertBefore(element, unorganisedList.children[0]);

        element.classList.add("important")
        // console.log(element)
    }nnn
    ///
if (fast.textContents ="Fast and Furious") ;
("fast").prependTo("ul");


const fAndFirst = (node) => {
      if(isFandF(node.innerHTML) && !isFristChild(node)) {
          const parent = node.parentNode;
          parent.insertBefore(node, parent.firstChild);
          if(!node.classList.contains("important")) {
            node.classList.add("important");
          }
        }
    };

    const shuffleAndKeepFAndFirst = (random) => {
      for(const childNode of childNodes) {
          childNode.parentNode.appendChild(childNode, childNode.parentNode.children[random]);
          fAndFirst(childNode);
        }
    };
    ////
/*for (const i= 0; i<  fast.length; i++){
  if(i.TEXT_NODE == "Fast and Furious"){
    console.log(fast);
    console.log(i).innerText;
        const fast = document.i.innerText;
        fast.appendChild(chhil);
        $("").prependTo("body");
  }}*/
  ///
/*const fa =document.querySelectorAll('li').forEach(function (item) {
    if (item.innerText == '') {
        console.log(item);
    }
});
///
document.querySelectorAll('a').forEach(function (item) {
    if (item.innerText == 'SearchingText') {
        console.dir(item);
    }
});
///
/*const noeudd = function(){if(ul.hasChildNodes()){
 const chhil = ul.childNodes;
for (const i = 0; i< chhil.length; i++){
    //if(i.child.nodeType === 1 ){console.log(i);}
    if(i.TEXT_NODE == "Fast and Furious"){
        console.log(i).innerText;
        const fast = document.i.innerText;
        fast.appendChild(chhil);
    }
console.log(noeudds)} }}
function fact(){
    const nodefast = document.querySelector('li');
    if(nodefast.innerText == "Fast and Furious")
    if(noden.nodeType !== Node.TEXT_NODE){

    }
}*/